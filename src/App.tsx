import React from "react";
import { Button } from "@material-ui/core";

export const App = () => {
  return (
    <div style={{ width: "80%", background: "gray", height: 100 }}>
      <Button variant="contained">Testing</Button>
    </div>
  );
};
